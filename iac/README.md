# Infrastructure as Code (IaC)

This folder contains all infrastructure automation for the AgentSprint project.

- `terraform/` — Terraform code for Azure resources
- `bicep/` — (future) Bicep templates
- `ansible/` — (future) Ansible playbooks

Add new folders for other IaC tools as needed.
